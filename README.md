# Learn NodeJS with projects

## Simple TODOs REST API

```shell script
$ docker build . -t todos-api
$ docker run -d -p 9000:9000 todos-api
```

---

### TODOs

- Listen to OS signals in Docker Container
- Add a real database
- Service Discovery using Eureka